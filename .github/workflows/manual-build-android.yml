name: Manual APK Build for Flet

on:
  workflow_dispatch:  # Ручной запуск по кнопке

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.11'

    - name: Install Flet
      run: pip install flet
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: 🧹 Очистка кэша и получение зависимостей Flutter
      run: |
        flutter clean
        # Загружаем webview_flutter: 4.0.5, указанный в pubspec.yaml
        flutter pub get
      
    - name: Build APK
      # Команда flet теперь использует свежий Flutter SDK и чистый кэш.
      run: flet build apk  

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-app-apk
        # Возвращаем более общий путь для большей надежности
        path: build/**/*.apk